const { intervalNumbersCountValidator } = require('./interval-numbers-count')
const { getStringIndexValidator } = require('./string-index')
module.exports={
    intervalNumbersCountValidator,
    getStringIndexValidator
}
