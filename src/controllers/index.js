const { getIntervalNumbersCount } = require("./interval-numbers-count")
const { getMinimumSteps } = require("./minimum-steps")
const { getStringIndex } = require("./string-index")

module.exports = {
    getMinimumSteps,
    getStringIndex,
    getIntervalNumbersCount
}
